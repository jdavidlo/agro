import { createTheme } from '@mui/material/styles'

const theme = createTheme({
    palette:{
        primary:{
            main: '#1b5e20'
        },
        background:{
            paper: '#fafafa',
            // default: '#eeeeee',
        },
    },
})

export default theme;